services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - gnucash_data:/data
      - ./backend:/app
    command: python3 -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload

volumes:
  gnucash_data:
